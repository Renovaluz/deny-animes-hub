<!DOCTYPE html>
<html lang="pt-BR" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.titulo || 'DenyAnimeHub' %> | O seu universo Otaku</title>

    <!-- Fontes e Ícones -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    
    <!-- CSS Externo e Favicon -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/1.ico" type="image/ico">
    
    <!-- ================================================================================================== -->
    <!--      ESTILOS EMBUTIDOS PARA A NAVBAR DEFINITIVA - À PROVA DE FALHAS                                -->
    <!-- ================================================================================================== -->
    <style>
        :root {
            /* Definindo variáveis de fallback caso o CSS externo falhe */
            --bg-secondary: #141414;
            --text-primary: #F5F5F5;
            --text-secondary: #A0A0A0;
            --accent-primary: #E50914;
            --border-color: rgba(255, 255, 255, 0.1);
            --header-height: 70px;
        }

        html[data-theme='light'] {
            --bg-secondary: #FFFFFF;
            --text-primary: #1C1C1E;
            --text-secondary: #636366;
            --border-color: #D1D1D6;
        }

        /* --- Força a base do corpo para funcionar com o header fixo --- */
        body {
            padding-top: var(--header-height);
        }
        body.nav-open {
            overflow: hidden;
        }

        /* --- HEADER (NAVBAR) --- */
        .site-header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: var(--header-height);
            z-index: 1000;
            background-color: rgba(var(--bg-secondary-rgb, 20, 20, 20), 0.7);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border-color);
        }
        html[data-theme='light'] .site-header { --bg-secondary-rgb: 255, 255, 255; }
        html[data-theme='dark'] .site-header { --bg-secondary-rgb: 20, 20, 20; }
        
        .site-header .container {
            height: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        /* --- LOGO --- */
        .site-header__logo a {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 700;
            font-size: 1.25rem;
        }
        .site-header__logo img { height: 40px; }

        /* --- BOTÃO DE MENU ANIMADO --- */
        .menu-toggle-btn {
            display: none; /* Oculto por padrão no desktop */
            width: 44px;
            height: 44px;
            background-color: transparent;
            border: 1px solid var(--border-color);
            border-radius: 50%;
            cursor: pointer;
            z-index: 1002;
            transition: transform 0.3s ease, background-color 0.3s ease;
            padding: 10px;
        }
        .menu-toggle-btn .line {
            width: 100%;
            height: 2px;
            background-color: var(--text-primary);
            border-radius: 2px;
            transition: all 0.3s ease-in-out;
            display: block;
            margin: 4px 0;
        }
        .nav-open .menu-toggle-btn .line-1 { transform: translateY(6px) rotate(45deg); }
        .nav-open .menu-toggle-btn .line-2 { opacity: 0; }
        .nav-open .menu-toggle-btn .line-3 { transform: translateY(-6px) rotate(-45deg); }
        .menu-toggle-btn:hover { background-color: rgba(var(--accent-primary-rgb, 229, 9, 20), 0.1); }

        /* --- NAVEGAÇÃO (DESKTOP) --- */
        .site-header__nav ul {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            list-style: none;
            margin: 0;
            padding: 0;
        }
        .site-header__nav a:not(.btn) {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            position: relative;
            padding: 0.5rem 0;
        }
        .site-header__nav a:not(.btn)::after {
            content: '';
            position: absolute;
            bottom: -2px; left: 50%;
            transform: translateX(-50%);
            width: 0; height: 2px;
            background-color: var(--accent-primary);
            transition: width 0.2s ease;
        }
        .site-header__nav a:not(.btn):hover,
        .site-header__nav a:not(.btn).active { color: var(--text-primary); }
        .site-header__nav a:not(.btn):hover::after,
        .site-header__nav a:not(.btn).active::after { width: 100%; }

        /* --- OVERLAY DO MENU MOBILE --- */
        .page-overlay {
            position: fixed;
            inset: 0;
            background-color: rgba(0,0,0,0.5);
            z-index: 998;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.4s ease, visibility 0.4s ease;
        }
        body.nav-open .page-overlay {
            opacity: 1;
            visibility: visible;
        }

        /* --- RESPONSIVIDADE (A MÁGICA ACONTECE AQUI) --- */
        @media (max-width: 992px) {
            .menu-toggle-btn {
                display: block; /* Mostra o botão hambúrguer */
            }
            .site-header__nav {
                position: fixed;
                top: 0;
                right: -100%; /* Começa fora da tela */
                width: clamp(280px, 75vw, 340px);
                height: 100%;
                background-color: rgba(var(--bg-secondary-rgb, 20, 20, 20), 0.9);
                backdrop-filter: blur(15px);
                -webkit-backdrop-filter: blur(15px);
                padding: calc(var(--header-height) + 2rem) 1.5rem 2rem;
                transition: right 0.4s cubic-bezier(0.25, 1, 0.5, 1);
                z-index: 999;
                border-left: 1px solid var(--border-color);
            }
            .site-header__nav.is-active {
                right: 0; /* Desliza para dentro da tela */
            }
            .site-header__nav ul {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
                width: 100%;
            }
            .site-header__nav li { width: 100%; }
            .site-header__nav a {
                display: block;
                padding: 1rem;
                font-size: 1.1rem;
                border-radius: 8px;
            }
            .site-header__nav a:hover, .site-header__nav a.active {
                background-color: var(--bg-primary);
            }
            .site-header__nav a::after { display: none; }
        }
    </style>
</head>
<body class="page-<%= locals.page_name || 'default' %>">

    <header class="site-header">
        <div class="container">
            <div class="site-header__logo">
                <a href="/">
                    <img src="/images/1.ico" alt="Logo DenyAnimeHub">
                    <span>DenyAnimeHub</span>
                </a>
            </div>

            <!-- Botão de Menu (visível apenas em mobile) -->
            <button class="menu-toggle-btn" id="menu-toggle-btn" aria-label="Abrir menu" aria-expanded="false">
                <span class="line line-1"></span>
                <span class="line line-2"></span>
                <span class="line line-3"></span>
            </button>

            <!-- Navegação Principal -->
            <nav class="site-header__nav" id="main-nav">
                <ul>
                    <li><a href="/" class="<%= locals.page_name === 'index' ? 'active' : '' %>">Início</a></li>
                    <li><a href="/animes" class="<%= ['todos-animes', 'anime-detail'].includes(locals.page_name) ? 'active' : '' %>">Animes</a></li>
                    <li><a href="/noticias" class="<%= ['noticias', 'detalhe-post'].includes(locals.page_name) ? 'active' : '' %>">Notícias</a></li>
                    
                    <% if (userIsLoggedIn) { %>
                        <li><a href="/perfil" class="<%= ['perfil', 'editar-perfil'].includes(locals.page_name) ? 'active' : '' %>">Perfil</a></li>
                        <li><a href="/auth/logout" class="btn btn--small btn--primary">Sair</a></li>
                    <% } else { %>
                        <li><a href="/login" class="btn btn--small btn--primary <%= locals.page_name === 'login-page' ? 'active' : '' %>">Login</a></li>
                    <% } %>
                    
                    <li>
                        <button class="theme-switch" id="theme-toggle" aria-label="Alternar tema">
                            <i class="fas fa-moon icon-moon"></i>
                            <i class="fas fa-sun icon-sun"></i>
                        </button>
                    </li>
                </ul>
            </nav>
        </div>
    </header>
    
    <div class="page-overlay" id="page-overlay"></div>

    <main class="main-content-wrapper">
        <%- body %>
    </main>

    <%- include('../partials/footer') %>

    <script>
        // JAVASCRIPT À PROVA DE FALHAS, EMBUTIDO DIRETAMENTE NO LAYOUT
        document.addEventListener('DOMContentLoaded', () => {

            // --- LÓGICA DO MENU ---
            const menuToggleBtn = document.getElementById('menu-toggle-btn');
            const mainNav = document.getElementById('main-nav');
            const overlay = document.getElementById('page-overlay');
            const body = document.body;

            if (menuToggleBtn && mainNav && overlay) {
                const toggleMenu = () => {
                    const isActive = mainNav.classList.toggle('is-active');
                    body.classList.toggle('nav-open', isActive);
                    menuToggleBtn.setAttribute('aria-expanded', isActive);
                };

                menuToggleBtn.addEventListener('click', toggleMenu);
                overlay.addEventListener('click', toggleMenu);
            }

            // --- LÓGICA DO TEMA ---
            const themeToggleBtn = document.getElementById('theme-toggle');
            const htmlElement = document.documentElement;

            if (themeToggleBtn && htmlElement) {
                const applySavedTheme = () => {
                    htmlElement.setAttribute('data-theme', localStorage.getItem('theme') || 'dark');
                };
                const toggleTheme = () => {
                    const newTheme = htmlElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
                    htmlElement.setAttribute('data-theme', newTheme);
                    localStorage.setItem('theme', newTheme);
                };
                themeToggleBtn.addEventListener('click', toggleTheme);
                applySavedTheme();
            }
        });
    </script>

</body>
</html>