<!DOCTYPE html>
<html lang="pt-BR" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.titulo || 'DenyAnimeHub' %> | O seu universo Otaku</title>

    <!-- Fontes e Ícones -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    
    <!-- CSS Externo (para conteúdo das páginas) e Favicon -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/1.ico" type="image/ico">
    
    <!-- ================================================================================================== -->
    <!--      ESTILOS EMBUTIDOS DO LAYOUT DEFINITIVO - V10.0 "SIX PATHS SAGE MODE"                          -->
    <!-- ================================================================================================== -->
    <style>
        /* --- SISTEMA DE CORES GLOBAL E INFALÍVEL (AKATSUKI THEME) --- */
        :root {
            --header-height: 70px;
            --transition-smooth: 0.4s cubic-bezier(0.25, 1, 0.5, 1);
            --transition-fast: 0.2s ease;
            --border-radius: 8px;
        }

        /* --- TEMA ESCURO (Padrão) --- */
        html[data-theme='dark'] {
            --bg-primary: #0a0a0c;
            --bg-secondary: #18181c;
            --bg-secondary-rgb: 24, 24, 28;
            --bg-overlay: rgba(10, 10, 12, 0.85);
            --text-primary: #e8e8f0;
            --text-secondary: #a0a0b0;
            --accent-primary: #ff3b3b;
            --accent-primary-rgb: 255, 59, 59;
            --accent-secondary: #ff6b6b;
            --border-color: #3a3a44;
        }

        /* --- TEMA CLARO --- */
        html[data-theme='light'] {
            --bg-primary: #f4f4f8;
            --bg-secondary: #ffffff;
            --bg-secondary-rgb: 255, 255, 255;
            --bg-overlay: rgba(244, 244, 248, 0.85);
            --text-primary: #1f1f23;
            --text-secondary: #5a5a63;
            --accent-primary: #d32f2f;
            --accent-primary-rgb: 211, 47, 47;
            --accent-secondary: #b71c1c;
            --border-color: #d1d1d9;
        }

        /* --- BASE DO LAYOUT --- */
        body {
            background-color: var(--bg-primary);
            color: var(--text-primary);
            padding-top: var(--header-height);
            transition: background-color var(--transition-fast);
        }
        body.nav-open { overflow: hidden; }

        /* --- HEADER (NAVBAR) --- */
        .site-header {
            position: fixed; top: 0; left: 0;
            width: 100%; height: var(--header-height);
            z-index: 1000;
            background-color: rgba(var(--bg-secondary-rgb), 0.75);
            backdrop-filter: blur(15px); -webkit-backdrop-filter: blur(15px);
            border-bottom: 1px solid var(--border-color);
            transition: all var(--transition-fast);
        }
        .site-header .container {
            height: 100%; display: flex;
            justify-content: space-between; align-items: center;
        }
        .site-header__logo a {
            display: flex; align-items: center; gap: 0.75rem;
            text-decoration: none; color: var(--text-primary);
            font-weight: 700; font-size: 1.25rem;
        }
        .site-header__logo img { height: 40px; }

        /* --- NAVEGAÇÃO DESKTOP --- */
        .site-header__nav { display: block; }
        .site-header__nav ul { display: flex; align-items: center; gap: 1rem; list-style: none; margin: 0; padding: 0; }
        .site-header__nav a:not(.btn) { color: var(--text-secondary); text-decoration: none; font-weight: 500; position: relative; padding: 0.5rem 0.25rem; }
        .site-header__nav a:not(.btn)::after { content: ''; position: absolute; bottom: -2px; left: 50%; transform: translateX(-50%); width: 0; height: 2px; background-color: var(--theme-accent-primary); transition: width var(--transition-fast) ease; }
        .site-header__nav a:not(.btn):hover, .site-header__nav a:not(.btn).active { color: var(--theme-text-primary); }
        .site-header__nav a:not(.btn):hover::after, .site-header__nav a:not(.btn).active::after { width: 100%; }
        
        /* --- BOTÃO DE TEMA --- */
        .theme-switch {
            background: transparent; border: 1px solid var(--border-color);
            color: var(--text-secondary); width: 40px; height: 40px;
            border-radius: 50%; cursor: pointer; display: flex;
            align-items: center; justify-content: center; font-size: 1.2rem;
            transition: all var(--transition-fast);
        }
        .theme-switch:hover { color: var(--theme-accent-primary); border-color: var(--theme-accent-primary); transform: scale(1.1) rotate(15deg); }
        html[data-theme='dark'] .theme-switch .icon-sun { display: none; }
        html[data-theme='light'] .theme-switch .icon-moon { display: none; }

        /* --- BOTÃO DE MENU ANIMADO (MOBILE) --- */
        .menu-toggle-btn {
            display: none;
            width: 44px;
            height: 44px;
            background-color: #8B0000; /* Vermelho Akatsuki */
            border: 2px solid white;  /* Borda branca */
            border-radius: 8px;
            cursor: pointer;
            z-index: 1002;
            padding: 0;
            position: relative;
            transition: all 0.3s ease-in-out;
        }

        /* Linhas brancas no botão (hamburguer) */
        .menu-toggle-btn .line {
            width: 24px;
            height: 2px;
            background-color: white; /* Linhas brancas */
            border-radius: 2px;
            transition: all 0.3s ease-in-out;
            display: block;
            margin: 5px auto;
        }

        /* Hover para destaque */
        .menu-toggle-btn:hover {
            background-color: #a30000; /* Vermelho um pouco mais vivo no hover */
            box-shadow: 0 0 8px rgba(255, 255, 255, 0.6);
        }

        /* Animação do X */
        .nav-open .menu-toggle-btn .line-1 {
            transform: translateY(7px) rotate(45deg);
        }
        .nav-open .menu-toggle-btn .line-2 {
            opacity: 0;
        }
        .nav-open .menu-toggle-btn .line-3 {
            transform: translateY(-7px) rotate(-45deg);
        }

        /* --- MENU OVERLAY DE TELA CHEIA (MOBILE) --- */
        #fullscreen-nav {
            position: fixed; inset: 0;
            z-index: 999;
            background-color: var(--theme-bg-overlay);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            display: flex; flex-direction: column;
            align-items: center; justify-content: center;
            opacity: 0; visibility: hidden;
            transition: opacity 0.4s ease, visibility 0s 0.4s;
        }
        #fullscreen-nav.is-active { opacity: 1; visibility: visible; transition-delay: 0s; }
        #fullscreen-nav ul { list-style: none; text-align: center; padding: 0; }
        #fullscreen-nav li {
            opacity: 0; transform: translateY(25px);
            transition: opacity 0.4s ease, transform 0.4s ease;
        }
        #fullscreen-nav.is-active li { opacity: 1; transform: translateY(0); }
        /* Animação em cascata */
        #fullscreen-nav.is-active li:nth-child(1) { transition-delay: 0.1s; }
        #fullscreen-nav.is-active li:nth-child(2) { transition-delay: 0.15s; }
        #fullscreen-nav.is-active li:nth-child(3) { transition-delay: 0.2s; }
        #fullscreen-nav.is-active li:nth-child(4) { transition-delay: 0.25s; }
        #fullscreen-nav.is-active li:nth-child(5) { transition-delay: 0.3s; }

        #fullscreen-nav a {
            display: inline-block; padding: 0.8rem 1rem;
            font-size: 1.8rem; font-weight: 600;
            color: var(--theme-text-secondary); text-decoration: none;
            transition: color var(--transition-fast), transform var(--transition-fast);
        }
        #fullscreen-nav a:hover, #fullscreen-nav a.active {
            color: var(--theme-text-primary);
            transform: scale(1.05);
        }
        #fullscreen-nav .btn { font-size: 1.2rem; padding: 0.8rem 2rem; margin-top: 1rem; }
        
        /* --- RESPONSIVIDADE GERAL --- */
        @media (max-width: 992px) {
            .menu-toggle-btn { display: block; }
            .site-header__nav { display: none; }
        }
    </style>
</head>
<body class="page-<%= locals.page_name || 'default' %>">

    <header class="site-header">
        <div class="container">
            <div class="site-header__logo">
                <a href="/">
                    <img src="/images/1.ico" alt="Logo DenyAnimeHub">
                    <span>DenyAnimeHub</span>
                </a>
            </div>

            <!-- Navegação Desktop (escondida em mobile pelo CSS) -->
            <nav class="site-header__nav">
                <ul>
                    <li><a href="/" class="<%= locals.page_name === 'index' ? 'active' : '' %>">Início</a></li>
                    <li><a href="/animes" class="<%= ['todos-animes', 'anime-detail'].includes(locals.page_name) ? 'active' : '' %>">Animes</a></li>
                    <li><a href="/noticias" class="<%= ['noticias', 'detalhe-post'].includes(locals.page_name) ? 'active' : '' %>">Notícias</a></li>
                    
                    <% if (userIsLoggedIn) { %>
                        <li><a href="/perfil" class="<%= ['perfil', 'editar-perfil'].includes(locals.page_name) ? 'active' : '' %>">Perfil</a></li>
                        <li><a href="/auth/logout" class="btn btn--small btn--primary">Sair</a></li>
                    <% } else { %>
                        <li><a href="/login" class="btn btn--small btn--primary <%= locals.page_name === 'login-page' ? 'active' : '' %>">Login</a></li>
                    <% } %>
                    
                    <li>
                        <button class="theme-switch" id="desktop-theme-toggle" aria-label="Alternar tema">
                            <i class="fas fa-moon icon-moon"></i>
                            <i class="fas fa-sun icon-sun"></i>
                        </button>
                    </li>
                </ul>
            </nav>

            <!-- Botão de Menu (visível apenas em mobile) -->
            <button class="menu-toggle-btn" id="menu-toggle-btn" aria-label="Abrir menu" aria-expanded="false">
                <span class="line line-1"></span>
                <span class="line line-2"></span>
                <span class="line line-3"></span>
            </button>
        </div>
    </header>
    
    <!-- Menu de Tela Cheia (controlado por JS) -->
    <div id="fullscreen-nav">
        <ul>
            <li><a href="/" class="<%= locals.page_name === 'index' ? 'active' : '' %>">Início</a></li>
            <li><a href="/animes" class="<%= ['todos-animes', 'anime-detail'].includes(locals.page_name) ? 'active' : '' %>">Animes</a></li>
            <li><a href="/noticias" class="<%= ['noticias', 'detalhe-post'].includes(locals.page_name) ? 'active' : '' %>">Notícias</a></li>
            
            <% if (userIsLoggedIn) { %>
                <li><a href="/perfil" class="<%= ['perfil', 'editar-perfil'].includes(locals.page_name) ? 'active' : '' %>">Perfil</a></li>
                <li><a href="/auth/logout" class="btn btn--primary">Sair</a></li>
            <% } else { %>
                <li><a href="/login" class="btn btn--primary <%= locals.page_name === 'login-page' ? 'active' : '' %>">Login / Registro</a></li>
            <% } %>
        </ul>
    </div>

    <main class="main-content-wrapper">
        <%- body %>
    </main>

    <%- include('../partials/footer') %>

    <script>
        document.addEventListener('DOMContentLoaded', () => {

            // --- LÓGICA DO MENU DE TELA CHEIA ---
            const menuToggleBtn = document.getElementById('menu-toggle-btn');
            const fullscreenNav = document.getElementById('fullscreen-nav');
            const body = document.body;

            if (menuToggleBtn && fullscreenNav) {
                const toggleMenu = () => {
                    const isActive = fullscreenNav.classList.toggle('is-active');
                    body.classList.toggle('nav-open', isActive);
                    menuToggleBtn.setAttribute('aria-expanded', isActive);
                };
                menuToggleBtn.addEventListener('click', toggleMenu);
            }

            // --- LÓGICA DO TEMA (AGORA FUNCIONA PARA AMBOS OS BOTÕES) ---
            const themeToggleButtons = document.querySelectorAll('.theme-switch'); // Seleciona todos os botões de tema
            const htmlElement = document.documentElement;

            if (themeToggleButtons.length > 0 && htmlElement) {
                const applySavedTheme = () => {
                    htmlElement.setAttribute('data-theme', localStorage.getItem('theme') || 'dark');
                };

                const toggleTheme = () => {
                    const newTheme = htmlElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
                    htmlElement.setAttribute('data-theme', newTheme);
                    localStorage.setItem('theme', newTheme);
                };

                themeToggleButtons.forEach(btn => btn.addEventListener('click', toggleTheme));
                
                applySavedTheme();
            }
        });
    </script>
</body>
</html>