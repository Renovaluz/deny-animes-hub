<!DOCTYPE html>
<html lang="pt-BR" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.titulo || 'DenyAnimeHub' %> | O seu universo Otaku</title>

    <!-- Fontes e Ícones -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    
    <!-- CSS Externo e Favicon -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/1.ico" type="image/ico">
    
    <!-- ================================================================================================== -->
    <!--      ESTILOS EMBUTIDOS PARA O LAYOUT DEFINITIVO - V8.0 "PERFECT SUSANOO"                           -->
    <!-- ================================================================================================== -->
    <style>
        /* --- SISTEMA DE CORES COESO E À PROVA DE FALHAS --- */
        :root {
            /* Definindo variáveis de fallback caso o CSS externo falhe */
            --header-height: 70px;
            --transition-smooth: 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
            --transition-fast: 0.2s ease;
            --border-radius: 8px;
        }

        html[data-theme='dark'] {
            --theme-bg-primary: #0A0A0A;
            --theme-bg-secondary: #141414;
            --theme-bg-secondary-rgb: 20, 20, 20;
            --theme-text-primary: #F5F5F5;
            --theme-text-secondary: #A0A0A0;
            --theme-border-color: rgba(255, 255, 255, 0.1);
            --theme-accent-primary: #E50914;
        }

        html[data-theme='light'] {
            --theme-bg-primary: #F0F2F5;
            --theme-bg-secondary: #FFFFFF;
            --theme-bg-secondary-rgb: 255, 255, 255;
            --theme-text-primary: #1C1C1E;
            --theme-text-secondary: #636366;
            --theme-border-color: #D1D1D6;
            --theme-accent-primary: #D32F2F;
        }

        /* --- BASE DO LAYOUT --- */
        body {
            background-color: var(--theme-bg-primary);
            color: var(--theme-text-primary);
            padding-top: var(--header-height);
            transition: background-color var(--transition-fast), color var(--transition-fast);
        }
        body.nav-open {
            overflow: hidden;
        }

        /* --- HEADER (NAVBAR) --- */
        .site-header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: var(--header-height);
            z-index: 1000;
            background-color: rgba(var(--theme-bg-secondary-rgb), 0.75);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--theme-border-color);
            transition: background-color var(--transition-fast), border-color var(--transition-fast);
        }
        
        .site-header .container {
            height: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .site-header__logo a {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
            color: var(--theme-text-primary);
            font-weight: 700;
            font-size: 1.25rem;
        }
        .site-header__logo img { height: 40px; }

        /* --- NAVEGAÇÃO DESKTOP --- */
        .site-header__nav {
            display: flex; /* Visível por padrão */
        }
        .site-header__nav ul {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            list-style: none;
            margin: 0;
            padding: 0;
        }
        .site-header__nav a:not(.btn) {
            color: var(--theme-text-secondary);
            text-decoration: none;
            font-weight: 500;
            position: relative;
            padding: 0.5rem 0;
        }
        .site-header__nav a:not(.btn)::after {
            content: '';
            position: absolute;
            bottom: -2px; left: 50%;
            transform: translateX(-50%);
            width: 0; height: 2px;
            background-color: var(--theme-accent-primary);
            transition: width var(--transition-fast) ease;
        }
        .site-header__nav a:not(.btn):hover,
        .site-header__nav a:not(.btn).active { color: var(--theme-text-primary); }
        .site-header__nav a:not(.btn):hover::after,
        .site-header__nav a:not(.btn).active::after { width: 100%; }

        /* --- BOTÃO DE MENU ANIMADO (MOBILE) --- */
        .menu-toggle-btn {
            display: none; /* Oculto no desktop */
            width: 44px;
            height: 44px;
            background-color: transparent;
            border: none;
            cursor: pointer;
            z-index: 1002;
            padding: 0;
        }
        .menu-toggle-btn .line {
            width: 24px;
            height: 2px;
            background-color: var(--theme-text-primary);
            border-radius: 2px;
            transition: all 0.3s ease-in-out;
            display: block;
            margin: 5px auto;
        }
        .nav-open .menu-toggle-btn .line-1 { transform: translateY(7px) rotate(45deg); }
        .nav-open .menu-toggle-btn .line-2 { opacity: 0; }
        .nav-open .menu-toggle-btn .line-3 { transform: translateY(-7px) rotate(-45deg); }

        /* --- OVERLAY DO MENU MOBILE --- */
        .page-overlay {
            position: fixed;
            inset: 0;
            background-color: rgba(0,0,0,0.5);
            z-index: 998;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.4s ease, visibility 0s 0.4s;
        }
        body.nav-open .page-overlay {
            opacity: 1;
            visibility: visible;
            transition-delay: 0s;
        }

        /* --- RESPONSIVIDADE E MENU MOBILE SOFISTICADO --- */
        @media (max-width: 992px) {
            .menu-toggle-btn { display: block; } /* Mostra o botão */
            
            .site-header__nav {
                position: fixed;
                top: 0;
                right: 0;
                width: clamp(280px, 75vw, 340px);
                height: 100%;
                padding: calc(var(--header-height) + 2rem) 1.5rem 2rem;
                
                background-color: rgba(var(--theme-bg-secondary-rgb), 0.85);
                backdrop-filter: blur(15px);
                -webkit-backdrop-filter: blur(15px);
                
                border-left: 1px solid var(--theme-border-color);
                z-index: 999;
                
                transform: translateX(100%);
                transition: transform 0.4s var(--transition-smooth);
            }
            .site-header__nav.is-active {
                transform: translateX(0);
            }

            /* Estilos dos itens de menu no mobile */
            .site-header__nav ul {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
                width: 100%;
            }
            .site-header__nav li { 
                width: 100%;
                opacity: 0;
                transform: translateX(20px);
                transition: opacity 0.4s ease, transform 0.4s ease;
            }
            /* Animação em cascata */
            .site-header__nav.is-active li { opacity: 1; transform: translateX(0); }
            .site-header__nav.is-active li:nth-child(1) { transition-delay: 0.1s; }
            .site-header__nav.is-active li:nth-child(2) { transition-delay: 0.15s; }
            .site-header__nav.is-active li:nth-child(3) { transition-delay: 0.2s; }
            .site-header__nav.is-active li:nth-child(4) { transition-delay: 0.25s; }
            .site-header__nav.is-active li:nth-child(5) { transition-delay: 0.3s; }

            .site-header__nav a {
                display: block;
                width: 100%;
                padding: 1rem;
                font-size: 1.1rem;
                font-weight: 500;
                border-radius: var(--border-radius);
                transition: background-color var(--transition-fast), color var(--transition-fast);
            }
            .site-header__nav a:not(.btn):hover, .site-header__nav a:not(.btn).active {
                background-color: rgba(var(--theme-accent-primary-rgb, 229, 9, 20), 0.1);
                color: var(--theme-accent-primary);
            }
            .site-header__nav a:not(.btn)::after { display: none; }
        }
    </style>
</head>
<body class="page-<%= locals.page_name || 'default' %>">

    <header class="site-header">
        <div class="container">
            <div class="site-header__logo">
                <a href="/">
                    <img src="/images/1.ico" alt="Logo DenyAnimeHub">
                    <span>DenyAnimeHub</span>
                </a>
            </div>

            <!-- Botão de Menu (visível apenas em mobile) -->
            <button class="menu-toggle-btn" id="menu-toggle-btn" aria-label="Abrir menu" aria-expanded="false">
                <span class="line line-1"></span>
                <span class="line line-2"></span>
                <span class="line line-3"></span>
            </button>

            <!-- Navegação Principal (agora uma única estrutura) -->
            <nav class="site-header__nav" id="main-nav">
                <ul>
                    <li><a href="/" class="<%= locals.page_name === 'index' ? 'active' : '' %>">Início</a></li>
                    <li><a href="/animes" class="<%= ['todos-animes', 'anime-detail'].includes(locals.page_name) ? 'active' : '' %>">Animes</a></li>
                    <li><a href="/noticias" class="<%= ['noticias', 'detalhe-post'].includes(locals.page_name) ? 'active' : '' %>">Notícias</a></li>
                    
                    <% if (userIsLoggedIn) { %>
                        <li><a href="/perfil" class="<%= ['perfil', 'editar-perfil'].includes(locals.page_name) ? 'active' : '' %>">Perfil</a></li>
                        <li><a href="/auth/logout" class="btn btn--small btn--primary">Sair</a></li>
                    <% } else { %>
                        <li><a href="/login" class="btn btn--small btn--primary <%= locals.page_name === 'login-page' ? 'active' : '' %>">Login</a></li>
                    <% } %>
                    
                    <li>
                        <button class="theme-switch" id="theme-toggle" aria-label="Alternar tema">
                            <i class="fas fa-moon icon-moon"></i>
                            <i class="fas fa-sun icon-sun"></i>
                        </button>
                    </li>
                </ul>
            </nav>
        </div>
    </header>
    
    <div class="page-overlay" id="page-overlay"></div>

    <main class="main-content-wrapper">
        <%- body %>
    </main>

    <%- include('../partials/footer') %>

    <script>
        document.addEventListener('DOMContentLoaded', () => {

            // --- LÓGICA DO MENU ---
            const menuToggleBtn = document.getElementById('menu-toggle-btn');
            const mainNav = document.getElementById('main-nav');
            const overlay = document.getElementById('page-overlay');
            const body = document.body;

            if (menuToggleBtn && mainNav && overlay) {
                const toggleMenu = () => {
                    mainNav.classList.toggle('is-active');
                    body.classList.toggle('nav-open');
                    menuToggleBtn.setAttribute('aria-expanded', mainNav.classList.contains('is-active'));
                };
                menuToggleBtn.addEventListener('click', toggleMenu);
                overlay.addEventListener('click', toggleMenu);
            }

            // --- LÓGICA DO TEMA ---
            const themeToggleBtn = document.getElementById('theme-toggle');
            const htmlElement = document.documentElement;

            if (themeToggleBtn && htmlElement) {
                const applySavedTheme = () => {
                    const savedTheme = localStorage.getItem('theme') || 'dark';
                    htmlElement.setAttribute('data-theme', savedTheme);
                };
                const toggleTheme = () => {
                    const newTheme = htmlElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
                    htmlElement.setAttribute('data-theme', newTheme);
                    localStorage.setItem('theme', newTheme);
                };
                themeToggleBtn.addEventListener('click', toggleTheme);
                applySavedTheme();
            }
        });
    </script>
</body>
</html>